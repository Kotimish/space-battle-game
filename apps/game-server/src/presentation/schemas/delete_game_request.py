from pydantic import BaseModel, Field


class DeleteGameRequest(BaseModel):
    # game_id: str = Field(..., description="ID игры, которую нужно завершить.")
    # user_id: str = Field(..., description="ID пользователя, инициирующего удаление.")
    pass
